{
  "name": "y",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "verify": "bash scripts/verify-build.sh",
    "deploy:preview": "npm run build && npx wrangler pages deploy dist --project-name=qurbani-usa --branch=dev",
    "deploy:production": "npm run build && npx wrangler pages deploy dist --project-name=qurbani-usa --branch=main",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.12",
    "@supabase/supabase-js": "^2.97.0",
    "@tailwindcss/vite": "^4.2.0",
    "astro": "^5.17.1",
    "dotenv": "^17.3.1",
    "pg": "^8.18.0",
    "stripe": "^20.3.1",
    "tailwindcss": "^4.2.0"
  }
}
